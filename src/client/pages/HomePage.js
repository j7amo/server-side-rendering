import React from 'react';

const HomePage = () => {
  return (
    <div>
      <div>Home!</div>
      <button onClick={() => console.log('Hi there')}>Press me</button>
    </div>
  );
};

export default HomePage;
